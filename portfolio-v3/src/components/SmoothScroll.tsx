"use client";

// SmoothScroll component - currently disabled
// Can be re-enabled with Lenis if smooth scrolling is needed
export function SmoothScroll() {
	// Smooth scrolling is currently disabled to prevent conflicts with Framer Motion
	// If you want to re-enable it, you can use Lenis here
	return null;
}
